[package]
name = "redis-evaljs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
name = "redisjs"

[dependencies]
rquickjs = { version = "0.6.2", features = ["parallel"] }
redis-module = "2.0.7"
